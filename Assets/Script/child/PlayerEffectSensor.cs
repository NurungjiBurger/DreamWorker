using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerEffectSensor : EffectSensor
{ 
    // Start is called before the first frame update
    void Start()
    {
        disappear = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
